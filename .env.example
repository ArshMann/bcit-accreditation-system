# Create a .env file in the root directory of your project and copy this content into it.
DB_NAME=accreditation_db
DB_USER=basic_user
DB_PASSWORD=proj1047
DB_HOST=localhost
DB_PORT=5432
CSRF_TRUSTED_ORIGINS=http://localhost
IN_DOCKER=false
ALLOWED_HOSTS=localhost,127.0.0.1
DEBUG=False
SECRET_KEY=your-secure-secret-key-here
FERNET_KEY=your-fernet-key-here


# Up until this comment is the template for the .env file for this Django project.


# ## Explanation for each section (its just reapeasted from above):
# # Django settings
# SECRET_KEY=replace-with-a-secure-key
# DEBUG=False
# ALLOWED_HOSTS=localhost,127.0.0.1

# # Database settings
# DB_NAME=accreditation_db
# DB_USER=basic_user
# DB_PASSWORD=proj1047
# DB_HOST=db
# DB_PORT=5432

# # Encryption settings - generate with: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
# FERNET_KEY=replace-with-a-generated-fernet-key